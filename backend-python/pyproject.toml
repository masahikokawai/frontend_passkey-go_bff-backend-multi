[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "integration: requires real docker-compose MySQL (excluded by default, run explicitly with -m integration)",
]
addopts = "-m 'not integration'"
